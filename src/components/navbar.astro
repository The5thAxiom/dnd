---
const currentPath: string = new URL(Astro.request.url).pathname;

type pageLink = {
    link: string;
    title: string;
};

const pages: pageLink[] = [
    {
        link: '/witcher-2022',
        title: 'Witcher 2022 Campaign'
    },
    {
        link: '/phandelver-2022',
        title: 'Phandelver 2022 Campaign'
    },
    {
        link: '/sufang-2023',
        title: 'Sufang 2023 Campaign'
    }
]
---
<style>
    
    nav {
        display: flex;
        justify-content: flex-end;
        align-items: stretch;
        background-color: maroon;
        color: white;
        padding-inline: 5vw;

        position: sticky;
        top: 0;

        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3), 0 0 15px rgba(0, 0, 0, 0.1),
        0 0 20px rgba(0, 0, 0, 0.05), 0 0 30px rgba(0, 0, 0, 0.042),
        0 0 40px rgba(0, 0, 0, 0.03), 0 0 80px rgba(0, 0, 0, 0.02);
    }

    *::selection {
        color: darkred;
        background-color: lightgray;
    }

    a::selection {
        color: black
    }

    a {
        color: whitesmoke;
        padding: 1em;
        
        display: flex;
        justify-content: center;
        align-items: center;
        
        text-decoration: none;
    }

    a:hover {
        background-color: darkred;
    }

    .active {
        color: yellow;
    }

    a.logo-link {
        margin-right: auto;
        gap: 0.5em;
    }

    img.logo {
        height: 2em;
    }

    @media only screen and (max-width: 600px) {
        nav {
            flex-direction: column;
            padding-inline: 0;
        }
        a {
            width: calc(100% - 2em);
        }
        a.logo-link {
            margin-right: 0;
        }
    }
</style>

<nav>
    <a
        href='/'
        class={ 'logo-link ' + (
                '/' === currentPath
                    ? 'active'
                    : 'inactive'
            )
        }
    >
    <img src="/d20.svg" class='logo' alt='logo'/>
        Sam's DnD
    </a>
    {pages.map(
        page =>
            <a
                href={page.link}
                class={
                    page.link === currentPath
                        ? 'active'
                        : 'inactive'
                }
            >
                {page.title}
            </a>
        )}
</nav>